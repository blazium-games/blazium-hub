# Copy to production.env (gitignored) and fill in values.
# Mirror the same file contents into the GitHub Actions secret PRODUCTION_ENV
# so admins can rotate keys without committing secrets.
#
# Generate a key:  openssl rand -hex 32
# Must be 64 hexadecimal characters for Blazium/Godot script encryption.

SCRIPT_ENCRYPTION_KEY=
